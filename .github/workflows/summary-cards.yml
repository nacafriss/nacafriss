name: GitHub Profile Summary Cards

on:
  schedule:
    - cron: '0 0 * * *' # Update setiap hari
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    name: generate-cards
    steps:
      - uses: arifszn/github-profile-summary-cards@main
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          USERNAME: ${{ github.repository_owner }}
